# Copy this file to .env and set values for local development.
# Never commit your real secrets (.env is ignored by .gitignore).

# Database credentials
POSTGRES_USER=fleet
POSTGRES_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=fleetdb
